# DSC diagram

Edit `dsc.drawio` in draw.io. Icons are embedded, from Microsoft's Azure icon collection.

Run `python3 scripts/export-dsc-diagram.py` to update both SVG exports.
The exporter supports this diagram's rectangles, text, icons and orthogonal arrows.
The page uses the still export when reduced motion is enabled.
